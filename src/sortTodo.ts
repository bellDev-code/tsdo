type SortType = "asc" | "desc";

// function sortTypeFunc(todolist: TodoItem[], type: SortType) {
//   switch(type) {
//     case 'asc':
//      return todolist.sort((a, b) => a.title - b.title)
//     case 'desc':
      
//   }
// }